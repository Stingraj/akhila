Replace all hero overlay content with this simplified layout. Remove all existing hero CSS and HTML elements (#hero-left-top, #hero-left-bottom, #hero-right, #hero-cta, #hero-scroll-hint, #hero-center-label, #hero-content, #hero-card and all their CSS).
Add this HTML inside #sticky-container, after #scroll-hint:
html<div id="hero-left">
  <h1 id="hero-title">Your gateway to<br>the extraordinary</h1>
  <p id="hero-desc">Every journey is crafted around your curiosity, time, and dreams — so you can focus on what truly matters, while we take care of everything else.</p>
</div>

<div id="hero-right">
  <div id="hero-right-line1">We are</div>
  <div id="hero-right-line2">destination</div>
</div>
Add this CSS. All values are designed for 1920px base and scale responsively using clamp():
css#hero-left {
  position: absolute;
  top: clamp(110px, 12.3vw, 236px);
  left: clamp(40px, 4.69vw, 90px);
  z-index: 10;
  pointer-events: none;
  max-width: clamp(280px, 28.125vw, 540px);
}

#hero-title {
  font-family: 'Bricolage Grotesque', 'Inter', sans-serif;
  font-size: clamp(36px, 3.75vw, 72px);
  font-weight: 500;
  line-height: 1.05;
  color: #fff;
  margin-bottom: clamp(16px, 1.875vw, 36px);
}

#hero-desc {
  font-family: 'Inter', sans-serif;
  font-size: clamp(13px, 1.25vw, 24px);
  font-weight: 400;
  line-height: 1.6;
  color: rgba(255,255,255,0.7);
}

#hero-right {
  position: absolute;
  bottom: clamp(112px, 12.7vw, 244px);
  right: clamp(40px, 4.69vw, 90px);
  z-index: 10;
  pointer-events: none;
  text-align: right;
}

#hero-right-line1 {
  font-family: 'Bricolage Grotesque', 'Inter', sans-serif;
  font-size: clamp(48px, 5.208vw, 100px);
  font-weight: 700;
  line-height: 0.95;
  color: #fff;
}

#hero-right-line2 {
  font-family: 'Bricolage Grotesque', 'Inter', sans-serif;
  font-size: clamp(48px, 5.208vw, 100px);
  font-weight: 400;
  font-style: italic;
  line-height: 0.95;
  color: #fff;
}
Update the hero fade-out animation in initScroll() — replace any existing hero gsap animation with:
jsconst heroEls = document.querySelectorAll('#hero-left, #hero-right');
if (heroEls.length) {
  gsap.to(heroEls, {
    y: -80,
    opacity: 0,
    ease: 'power2.in',
    scrollTrigger: {
      trigger: wrapper,
      start: 'top top',
      end: () => '+=' + (window.innerHeight * 7 * 0.32),
      scrub: 0.8,
    }
  });
}
Do not change any other scroll animations, canvas logic, window section, exploration section, or footer.