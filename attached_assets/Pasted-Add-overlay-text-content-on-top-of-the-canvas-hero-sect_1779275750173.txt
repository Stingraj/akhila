Add overlay text content on top of the canvas hero section (#sticky-container). Make it fully responsive based on 1920px as the base design. Do not change any existing scroll or canvas logic.
Add this HTML inside #sticky-container, after the #scroll-hint div:
html<div id="hero-content">
  <h1 id="hero-title">Your next adventure is just one door away</h1>
  <p id="hero-sub">Unique travel destinations await to spark curiosity and inspire your next adventure. Discover hidden gems around the world waiting to be explored.</p>
</div>

<div id="hero-card">
  <img src="/images/Moraine Lake Left.png" alt="Moraine Lake" id="hero-card-img">
  <div id="hero-card-text">
    <div id="hero-card-label">Reiseguides · Oct 27, 2024</div>
    <div id="hero-card-title">18 luxuriöse Urlaubsziele</div>
    <div id="hero-card-link">Jetzt lesen →</div>
  </div>
</div>
Add this CSS:
css#hero-content {
  position: absolute;
  top: clamp(120px, 13.5vw, 260px);
  left: clamp(40px, 4.7vw, 90px);
  right: clamp(40px, 4.7vw, 90px);
  z-index: 10;
  pointer-events: none;
}

#hero-title {
  font-family: 'Bricolage Grotesque', 'Inter', sans-serif;
  font-size: clamp(36px, 4.7vw, 90px);
  font-weight: 700;
  line-height: 1.0;
  color: #1a2a4a;
  max-width: clamp(320px, 35vw, 680px);
  margin: 0;
}

#hero-sub {
  font-family: 'Bricolage Grotesque', 'Inter', sans-serif;
  font-size: clamp(14px, 1.25vw, 24px);
  font-weight: 400;
  line-height: 1.5;
  color: #1a2a4a;
  max-width: clamp(280px, 25vw, 480px);
  margin-top: clamp(16px, 2vw, 40px);
  position: absolute;
  bottom: clamp(-420px, -28vw, -180px);
  left: 0;
}

#hero-card {
  position: absolute;
  bottom: clamp(40px, 4.2vw, 80px);
  right: clamp(40px, 4.7vw, 90px);
  z-index: 10;
  display: flex;
  align-items: center;
  gap: clamp(10px, 0.8vw, 16px);
  background: rgba(255,255,255,0.85);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border-radius: 12px;
  padding: clamp(8px, 0.6vw, 12px) clamp(12px, 1vw, 20px) clamp(8px, 0.6vw, 12px) clamp(8px, 0.6vw, 12px);
  width: clamp(220px, 16.7vw, 320px);
  pointer-events: none;
}

#hero-card-img {
  width: clamp(56px, 4.2vw, 80px);
  height: clamp(44px, 3.3vw, 64px);
  object-fit: cover;
  border-radius: 8px;
  flex-shrink: 0;
}

#hero-card-text {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

#hero-card-label {
  font-family: 'Inter', sans-serif;
  font-size: clamp(10px, 0.57vw, 11px);
  color: #888;
  letter-spacing: 0.05em;
}

#hero-card-title {
  font-family: 'Inter', sans-serif;
  font-size: clamp(12px, 0.73vw, 14px);
  font-weight: 600;
  color: #1a1a1a;
  line-height: 1.3;
}

#hero-card-link {
  font-family: 'Inter', sans-serif;
  font-size: clamp(11px, 0.63vw, 12px);
  color: #3b82f6;
  font-weight: 500;
}
Do not change any JavaScript, scroll animations, canvas logic, or other sections.