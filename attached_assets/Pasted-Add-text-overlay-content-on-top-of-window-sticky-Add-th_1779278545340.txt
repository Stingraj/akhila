Add text overlay content on top of #window-sticky. Add this HTML inside #window-sticky, after the #airplane-window div and before #cinematic-text:
html<div id="window-hero-left">
  <h1 id="window-hero-title">Your gateway to<br>the extraordinary</h1>
  <p id="window-hero-desc">Every journey is crafted around your curiosity, time, and dreams — so you can focus on what truly matters, while we take care of everything else.</p>
</div>

<div id="window-hero-right">
  <div id="window-hero-line1">We are</div>
  <div id="window-hero-line2">destination</div>
</div>
Add this CSS:
css#window-hero-left {
  position: absolute;
  top: clamp(110px, 12.3vw, 236px);
  left: clamp(40px, 4.69vw, 90px);
  z-index: 20;
  pointer-events: none;
  max-width: clamp(280px, 28.125vw, 540px);
}
#window-hero-title {
  font-family: 'Bricolage Grotesque', 'Inter', sans-serif;
  font-size: clamp(36px, 3.75vw, 72px);
  font-weight: 500;
  line-height: 1.05;
  color: #fff;
  margin-bottom: clamp(16px, 1.875vw, 36px);
}
#window-hero-desc {
  font-family: 'Inter', sans-serif;
  font-size: clamp(13px, 1.25vw, 24px);
  font-weight: 400;
  line-height: 1.6;
  color: rgba(255,255,255,0.7);
}
#window-hero-right {
  position: absolute;
  bottom: clamp(112px, 12.7vw, 244px);
  right: clamp(40px, 4.69vw, 90px);
  z-index: 20;
  pointer-events: none;
  text-align: right;
}
#window-hero-line1 {
  font-family: 'Bricolage Grotesque', 'Inter', sans-serif;
  font-size: clamp(48px, 5.208vw, 100px);
  font-weight: 700;
  line-height: 0.95;
  color: #fff;
}
#window-hero-line2 {
  font-family: 'Bricolage Grotesque', 'Inter', sans-serif;
  font-size: clamp(48px, 5.208vw, 100px);
  font-weight: 400;
  font-style: italic;
  line-height: 0.95;
  color: #fff;
}
Add fade-out animation inside initWindowSection(), after the clouds and window zoom animations:
jsconst windowHeroEls = document.querySelectorAll('#window-hero-left, #window-hero-right');
if (windowHeroEls.length) {
  gsap.to(windowHeroEls, {
    y: -80,
    opacity: 0,
    ease: 'power2.in',
    scrollTrigger: {
      trigger: section,
      start: 'top top',
      end: () => '+=' + (window.innerHeight * 0.8),
      scrub: 0.8,
    }
  });
}
Do not change canvas section, exploration section, destinations section, or any other existing logic.